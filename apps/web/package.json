{
  "name": "@reelworx/web",
  "version": "0.1.0",
  "private": true,
  "comment": "Placeholder. Claude Code: scaffold a Next.js (App Router) app here for the company/employer experience — see /CLAUDE.md and /docs/DESIGN_LANGUAGE.md before building UI. This app consumes @reelworx/shared for types, theme tokens, and the Prisma client.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "next lint"
  },
  "dependencies": {
    "@reelworx/shared": "*"
  }
}
